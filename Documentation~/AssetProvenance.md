# Match Rival Asset Provenance

Cat Merge Cafe owns its production Match Rival prefab/image baseline under `Assets/_Project/Content/MatchRival`.

Version `0.4.2` removes package copies that were not reachable from the consuming project's asset graph. Remaining package Resources icons, shared Indicator, and dependencies preserve their existing bytes, metadata, and GUIDs.
